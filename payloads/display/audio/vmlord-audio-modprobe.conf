# Shipped at /etc/modprobe.d/vmlord-audio.conf
# One cable rather than the default two: the second appears in GNOME as a
# duplicate output that plays into nothing anybody is capturing.
options snd-aloop index=0 pcm_substreams=1
