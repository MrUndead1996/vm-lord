# The size the guest's console and its compositor come up at before anything
# has resized the output. The host writes this file into the guest with the
# size the VM was configured for.
options vmlord_drm width=1920 height=1080
