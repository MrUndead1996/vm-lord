[package]
name = "vmlord-legacy-backend"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
libloading = "0.9"
vmlord-core = { path = "../core" }

[lints.rust]
unsafe_code = "allow"

[lints.clippy]
all = { level = "warn", priority = -1 }
