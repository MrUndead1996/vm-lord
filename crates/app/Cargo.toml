[package]
name = "vmlord-app"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
tracing.workspace = true
vmlord-core = { path = "../core" }

[lints]
workspace = true

[dev-dependencies]
semver.workspace = true
vmlord-core = { path = "../core", features = ["test-profile"] }
# The scoped subscriber the diagnostics tests install: records are read from a
# sink rather than returned, so a test needs somewhere to read them.
tracing-subscriber.workspace = true
