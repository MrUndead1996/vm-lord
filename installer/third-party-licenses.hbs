VMLord -- third-party licences
==============================

VMLord itself is distributed under the GNU General Public License, version 3
or later; its text is in the LICENSE file beside this one.

The components listed below are the work of other authors and are distributed
under their own terms, reproduced in full. Nothing here grants rights over
VMLord, and nothing in LICENSE takes rights away from these components.

{{!-- Every value below is written with a triple stache, because handlebars
escapes for HTML by default and this is a plain text file: with two braces, a
licence saying "AS IS" reaches the reader with its quotes turned into entities.
This note is a handlebars comment so that it explains the rule without being
copied into the notices -- an earlier version stated it in the header, and the
CI guard rightly failed on the very entities the sentence was quoting. --}}
Summary
-------
{{#each overview}}
  * {{{name}}} ({{{id}}}) -- {{count}} crate(s)
{{/each}}

The sections that follow are one per licence file, not one per licence: two
crates offering MIT name different copyright holders in it, so each text is
reproduced as that crate ships it rather than deduplicated into one.

{{#each licenses}}
================================================================================
{{{name}}}{{#if id}} ({{{id}}}){{/if}}

Applies to:
{{#each used_by}}
  * {{{crate.name}}} {{{crate.version}}}{{#if crate.repository}} -- {{{crate.repository}}}{{/if}}
{{/each}}

{{{text}}}

{{/each}}
================================================================================
End of third-party licences.
